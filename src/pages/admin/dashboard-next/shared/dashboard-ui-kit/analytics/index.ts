/**
 * Analytics composition helpers — full-width storytelling blocks.
 * Chart chrome lives in `../charts`; this module names the story roles.
 */

export {
  AnalyticsChart,
  ChartContainer,
  ChartHeader,
  ChartToolbar,
  FullWidthChart,
  FunnelContainer,
  HeatmapContainer,
  HeroChart,
  TrendContainer,
} from '../charts'

export type { ChartContainerProps, ChartHeaderProps, ChartToolbarProps } from '../charts'
